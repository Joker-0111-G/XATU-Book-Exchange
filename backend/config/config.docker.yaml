server:
  port: 8080
  mode: release          # Docker 环境用 release 模式

mysql:
  host: mysql             # Docker 服务名，而非 127.0.0.1
  port: 3306
  user: root
  password: "xatu_root_2026"
  dbname: xatu_book_exchange
  charset: utf8mb4
  max_idle_conns: 10
  max_open_conns: 100

redis:
  host: redis             # Docker 服务名
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "xatu-book-exchange-docker-secret-2026"
  access_expire: 2h
  refresh_expire: 168h

upload:
  max_size: 5
  dir: "./uploads/images"
  allow_types:
    - ".jpg"
    - ".jpeg"
    - ".png"
    - ".webp"
